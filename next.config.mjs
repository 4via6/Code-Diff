/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'standalone',  // Enable standalone output
  experimental: {
    // Enable if needed
    // serverActions: true,
  },
};

export default nextConfig;
